// use image_proc::binary_descriptors::brief::brief;
// use image_proc::binary_descriptors::match_binary_descriptors;

pub fn run() {
    /*
    let image1_descriptors = brief();
    log::debug!("match_binary_descriptors: {:?}", image1_descriptors);

    let result = match_binary_descriptors();
    log::debug!("match_binary_descriptors: {:?}", result);
     */
}
